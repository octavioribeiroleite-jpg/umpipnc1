name: Redesign society selector (retired)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  information:
    runs-on: ubuntu-latest
    steps:
      - name: Preserve the reviewed application source
        run: echo "This one-time source rewrite is retired. Use Validar Renovo IPNC for build and regression checks."
